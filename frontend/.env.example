# URL de l'API Django REST de LegalFlow.
# Le frontend ne parle jamais à cette URL depuis le navigateur : seules
# les route handlers Next (côté serveur) l'appellent. Voir README.md.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
